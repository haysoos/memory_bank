<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.memorybank" >

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:name=".MemoryBankApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activities.MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MemoriesListActivity"
            android:label="@string/title_activity_memories_list" >
        </activity>
        <activity
            android:name=".activities.TagsActivity"
            android:label="@string/title_activity_tags" >
        </activity>
        <activity
            android:name=".activities.EditTagActivity"
            android:label="@string/title_activity_create_tag"
            android:screenOrientation="portrait" >
        </activity>
        <activity
            android:name=".activities.MemoryTagsMapActivity"
            android:label="@string/title_activity_memory_tags_map" >
        </activity>
        <activity
            android:name=".activities.EditMemoryActivity"
            android:label="@string/title_activity_memories_list" >
        </activity>
    </application>

</manifest>
